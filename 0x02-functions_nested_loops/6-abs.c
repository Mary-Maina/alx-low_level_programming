#include "main.h"
/**
 * _abs- Computes the absolute of a number
 * @n: The integer to be checked
 *
 * Return: Absolte value
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
