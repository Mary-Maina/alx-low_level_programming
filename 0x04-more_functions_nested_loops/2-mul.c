#include "main.h"
/**
 * mul(int a, int b)- Mutiplies two numbers
 * @a: The first number
 * @b: The second number
 * Return: mutiplication of two numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
